name: Pre-build Client Assets

# Runs on push to main — rebuilds .client-assets/ and commits
# so the Manus sandbox never needs to run Vite build (avoids OOM).
# NOTE: This is now independent of CI to avoid stale assets when CI has
# pre-existing failures. The Dockerfile also runs a fresh Vite build as
# a safety net.
on:
  push:
    branches: [main]
    paths:
      - 'client/**'
      - 'shared/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'vite.config.*'
      - 'tailwind.config.*'

  # Manual trigger for on-demand rebuilds
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: prebuild-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_OPTIONS: "--max-old-space-size=4096"

jobs:
  prebuild:
    name: Rebuild .client-assets
    runs-on: self-hosted
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4
        with:
          # Need full history for commit/push
          fetch-depth: 0
          # Use PAT for push access (GITHUB_TOKEN may lack write permissions)
          token: ${{ secrets.PAT_TOKEN || secrets.GITHUB_TOKEN }}

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build client bundle
        run: |
          npx vite build
          echo "Vite build complete"

      - name: Generate build manifest
        run: |
          echo "{" > dist/public/.build-manifest.json
          echo "  \"builtAt\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"," >> dist/public/.build-manifest.json
          echo "  \"commit\": \"${{ github.sha }}\"," >> dist/public/.build-manifest.json
          echo "  \"ref\": \"${{ github.ref }}\"" >> dist/public/.build-manifest.json
          echo "}" >> dist/public/.build-manifest.json
          cat dist/public/.build-manifest.json

      - name: Update .client-assets
        run: |
          # Remove old assets
          rm -rf .client-assets
          # Copy new build
          cp -r dist/public .client-assets
          echo ".client-assets updated"
          echo ""
          echo "Asset summary:"
          ls -la .client-assets/assets/ | head -20
          echo ""
          echo "Total size:"
          du -sh .client-assets/

      - name: Commit and push .client-assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          git add .client-assets/
          
          # Only commit if there are changes
          if git diff --cached --quiet; then
            echo "No changes to .client-assets — skipping commit"
          else
            CHANGED=$(git diff --cached --stat | tail -1)
            git commit -m "chore: rebuild .client-assets [skip ci] from ${{ github.sha }}"
            git push
            echo "client-assets committed and pushed"
          fi
