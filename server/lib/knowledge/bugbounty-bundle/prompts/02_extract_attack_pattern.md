# Prompt: Normalized Record → Attack Pattern (Reusable)

From a normalized report, extract a reusable pattern:
- signals (observations)
- hypothesis
- safe tests (non-destructive)
- likely impact
- fixes

Return JSON matching attack_pattern.schema.json.