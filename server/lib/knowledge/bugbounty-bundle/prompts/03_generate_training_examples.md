# Prompt: Attack Pattern → Training Examples (Reasoning)

Generate 3-5 training examples that teach reasoning:
Input: observations + scope statement + tech context
Output: hypotheses + next tests + impact summary + remediation + OWASP + confidence

Return JSONL records matching training_example.schema.json.