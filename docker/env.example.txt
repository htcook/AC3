# ─────────────────────────────────────────────────────────────────────────────
# AC3 Platform — Local Development Environment Variables
# Copy to .env in project root and fill in your values
# ─────────────────────────────────────────────────────────────────────────────

# MySQL (docker-compose)
MYSQL_ROOT_PASSWORD=ac3-local-dev-only
MYSQL_PASSWORD=ac3-local-dev-only

# Application
JWT_SECRET=local-dev-jwt-secret-change-in-production
# Signs/verifies all session JWTs. Auth resolves CALDERA_JWT_SECRET, then falls
# back to JWT_SECRET; there is no committed default, so at least one MUST be set
# in production or auth fails closed. Use a long random value.
CALDERA_JWT_SECRET=local-dev-caldera-jwt-secret-change-in-production
# Service-account login password (red/blue/admin). No hardcoded/legacy password
# exists — this (or CALDERA_API_KEY) is the only way to log in.
CALDERA_PASSWORD=
# Only if white-label licensing is used: signs license tokens. Falls back to
# JWT_SECRET, then fails closed in production if neither is set.
WL_LICENSE_SIGNING_SECRET=

# AWS Credentials (for S3 access from local container)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=

# S3 Buckets
S3_EVIDENCE_BUCKET=ac3-dev-evidence-808038814732
S3_REPORTS_BUCKET=ac3-dev-reports-808038814732
S3_ASSETS_BUCKET=ac3-dev-assets-808038814732

# External APIs
OPENAI_API_KEY=
CALDERA_API_KEY=
CALDERA_BASE_URL=
SHODAN_API_KEY=
NVD_API_KEY=
CENSYS_API_ID=
CENSYS_API_SECRET=
ABUSEIPDB_API_KEY=
SECURITYTRAILS_API_KEY=
URLSCAN_API_KEY=

# LocalStack (for offline S3 emulation)
# AWS_ENDPOINT_URL=http://localhost:4566
# AWS_ACCESS_KEY_ID=test
# AWS_SECRET_ACCESS_KEY=test
