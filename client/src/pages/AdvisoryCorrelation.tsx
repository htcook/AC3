import ArsenalDashboard from "./ArsenalDashboard";

// Route to the Arsenal Dashboard with advisory-correlation tab pre-selected
export default function AdvisoryCorrelation() {
  return <ArsenalDashboard />;
}
