import ArsenalDashboard from "./ArsenalDashboard";

// Route to the Arsenal Dashboard with utility-playbooks tab pre-selected
export default function UtilityPlaybooks() {
  return <ArsenalDashboard />;
}
